# Group endpoints for targets pipeline

Group endpoints for targets pipeline

## Usage

``` r
group_ep_for_targets(ep, n_per_group)
```

## Arguments

- ep:

  A data.table containing the endpoint specification

- n_per_group:

  Number of endpoints per group.

## Value

A data.table containing the endpoint specification with grouping column.
