# Validate user inputs to endpoint definition

Validate user inputs to endpoint definition

## Usage

``` r
validate_endpoints_def(endpoint_base)
```

## Arguments

- endpoint_base:

  Endpoint definition data.table

## Value

Run for side effects. If there is a validation error it stops the
program, otherwise the function returns a 'TRUE'
